public class L3_string_Length {
    public static void main(String[] args) {
        String str = "This is a beautiful day today and i dont want to waste it sleeping all day";
    
        int str_length = str.length();
        System.out.println("the length of the string is : " + str_length);

        // in array to find the length we use array.length; not array.langth(); i.e paranthesis is not used as in array length is a property not a function 

        // in string to find the length we use str.length() not just str.length because in strings .length is a function not a property. so to call a funciton we need parenthesis;
    
    }

    
}
